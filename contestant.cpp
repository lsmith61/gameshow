#include "contestant.h"

void Contestant::insertContestant(int id, int points){
    // first call constrcutor to make an object
    // second add it to array
    Contestant contestantObj(id, points);
    
}